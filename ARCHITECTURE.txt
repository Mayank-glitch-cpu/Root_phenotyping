================================================================================
                    ROOT PHENOTYPING - DOCKER ARCHITECTURE
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│                              USER INTERFACE                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   Makefile Commands          Docker Compose           Shell Scripts          │
│   ├─ make build-all         ├─ docker-compose up     ├─ run_inference.sh   │
│   ├─ make train             │  train                 ├─ download_dataset.sh │
│   ├─ make inference         ├─ docker-compose up     └─ example_workflow.sh │
│   ├─ make clean             │  inference                                    │
│   └─ make help              └─ ...                                          │
│                                                                              │
└──────────────────────────────────────────┬───────────────────────────────────┘
                                           │
                                           ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           DOCKER CONTAINERS                                  │
├─────────────────────────────────┬───────────────────────────────────────────┤
│                                 │                                            │
│   TRAINING CONTAINER            │   INFERENCE CONTAINER                      │
│   ┌───────────────────────┐    │   ┌───────────────────────┐               │
│   │ Dockerfile.train      │    │   │ Dockerfile.inference  │               │
│   ├───────────────────────┤    │   ├───────────────────────┤               │
│   │ - Python 3.7 + Conda  │    │   │ - Python 3.7 + Conda  │               │
│   │ - CUDA 11.2 + cuDNN 8 │    │   │ - CUDA 11.2 + cuDNN 8 │               │
│   │ - TensorFlow GPU 2.4  │    │   │ - TensorFlow GPU 2.4  │               │
│   │ - Mask R-CNN Library  │    │   │ - Mask R-CNN Library  │               │
│   │ - Training.py         │    │   │ - inference.py        │               │
│   └───────────────────────┘    │   └───────────────────────┘               │
│             │                   │             │                              │
│             ▼                   │             ▼                              │
│   ┌───────────────────────┐    │   ┌───────────────────────┐               │
│   │ Training Pipeline     │    │   │ Inference Pipeline    │               │
│   ├───────────────────────┤    │   ├───────────────────────┤               │
│   │ 1. Download Dataset   │    │   │ 1. Load Model         │               │
│   │ 2. Load COCO Weights  │    │   │ 2. Process Images     │               │
│   │ 3. Train Model        │    │   │ 3. Detect Roots       │               │
│   │ 4. Save Checkpoints   │    │   │ 4. Calculate Metrics  │               │
│   │ 5. Save Final Model   │    │   │ 5. Generate Results   │               │
│   └───────────────────────┘    │   └───────────────────────┘               │
│                                 │                                            │
└─────────────────────────────────┴──────────────┬──────────────────────────────┘
                                                  │
                                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         PERSISTENT STORAGE (HOST)                            │
├──────────────────────┬──────────────────┬──────────────────┬────────────────┤
│                      │                  │                  │                │
│  📂 Root Images/     │  📂 models/      │  📂 logs/        │ 📂 test_images/│
│  Training dataset    │  Trained models  │  Checkpoints     │ Your images    │
│  (~5-8 GB)          │  (~245 MB)       │  (~1-2 GB)       │ (any size)     │
│                      │                  │                  │                │
│  Mounted to:         │  Mounted to:     │  Mounted to:     │ Mounted to:    │
│  /workspace/         │  /workspace/     │  /workspace/     │ /workspace/    │
│  Root Images/        │  models/         │  logs/           │ test_images/   │
│                      │                  │                  │                │
└──────────────────────┴──────────────────┴──────────────────┴────────────────┘
                                                  │
                                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              OUTPUT RESULTS                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   📂 inference_results/                                                      │
│   ├── 📄 all_results.csv                 (Detailed metrics for all images)  │
│   ├── 📄 summary_by_directory.json       (Summary statistics)               │
│   └── 📁 [subdirectories]/                                                  │
│       ├── 📊 [image]_result.png          (Visualization with overlays)      │
│       └── 📄 [subdir]_results.csv        (Per-directory results)            │
│                                                                              │
│   Key Metrics:                                                               │
│   - Number of roots detected                                                 │
│   - Root lengths (pixels)                                                    │
│   - Confidence scores                                                        │
│   - Bounding boxes                                                           │
│   - Segmentation masks                                                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

================================================================================
                              WORKFLOW DIAGRAM
================================================================================

TRAINING WORKFLOW:
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Build   │ -> │ Download │ -> │  Train   │ -> │   Save   │ -> │  Model   │
│  Image   │    │ Dataset  │    │  Model   │    │  Weights │    │  Ready   │
└──────────┘    └──────────┘    └──────────┘    └──────────┘    └──────────┘
     ↓               ↓                ↓                ↓               ↓
make build-all  auto or manual   GPU 3-6hrs      checkpoints    trained.h5


INFERENCE WORKFLOW:
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Build   │ -> │  Place   │ -> │   Run    │ -> │ Generate │ -> │  View    │
│  Image   │    │  Images  │    │Inference │    │ Results  │    │ Results  │
└──────────┘    └──────────┘    └──────────┘    └──────────┘    └──────────┘
     ↓               ↓                ↓                ↓               ↓
make build-all  test_images/    make inference   CSV + PNG      results/

================================================================================
                           COMMAND REFERENCE
================================================================================

SETUP & BUILD:
  make check          - Verify system requirements (Docker, GPU, etc.)
  make setup          - Create directory structure
  make build-all      - Build both training and inference images

TRAINING:
  make train          - Complete training pipeline (dataset + train + save)
  make logs           - View training logs
  make shell-train    - Interactive shell in training container

INFERENCE:
  make inference                        - Run on ./test_images/
  make inference TEST_DIR=./my_images   - Run on custom directory
  make test-inference                   - Quick test run
  make shell-inference                  - Interactive shell

UTILITIES:
  ./run_inference.sh -t ./images        - User-friendly inference wrapper
  ./example_workflow.sh                 - Interactive guided workflow
  ./download_dataset.sh                 - Download training dataset

CLEANUP:
  make clean          - Remove containers
  make clean-results  - Remove inference results
  make clean-all      - Remove everything (images, containers)

HELP:
  make help           - Show all commands

================================================================================
                        PERFORMANCE EXPECTATIONS
================================================================================

TRAINING:
┌──────────────────┬──────────────┬─────────────────┬────────────────┐
│   Hardware       │  Batch Size  │ Time per Epoch  │ Total (20 ep)  │
├──────────────────┼──────────────┼─────────────────┼────────────────┤
│ NVIDIA A100 40GB │      16      │    ~10 min      │   3-4 hours    │
│ NVIDIA V100 16GB │       8      │    ~15 min      │   5-6 hours    │
│ NVIDIA RTX 3090  │       8      │    ~18 min      │   6-7 hours    │
│ CPU (32 cores)   │       2      │   ~3-4 hours    │   2-3 DAYS     │
└──────────────────┴──────────────┴─────────────────┴────────────────┘

INFERENCE:
┌──────────────────┬─────────────────┬────────────────────┐
│   Hardware       │  Time per Image │   Throughput       │
├──────────────────┼─────────────────┼────────────────────┤
│ GPU              │    1-2 sec      │ ~1800 images/hour  │
│ CPU              │   10-15 sec     │  ~240 images/hour  │
└──────────────────┴─────────────────┴────────────────────┘

⚠️  GPU HIGHLY RECOMMENDED FOR TRAINING (20-30x faster than CPU)

================================================================================
                           RESOURCE REQUIREMENTS
================================================================================

DISK SPACE:
  - Docker Images:  ~15-18 GB
  - Dataset:        ~5-8 GB
  - Logs:           ~1-2 GB
  - TOTAL:          ~20-30 GB required

MEMORY:
  - Training:       16+ GB RAM (32 GB recommended)
  - Inference:      8+ GB RAM
  - GPU VRAM:       8+ GB for training

NETWORK:
  - Dataset Download: ~5-8 GB (one-time)
  - Docker Images:    ~15-18 GB (one-time)

================================================================================
                              FILE STRUCTURE
================================================================================

Root_phenotyping/
├── 🐳 DOCKER CONFIGURATION
│   ├── Dockerfile.train              # Training container definition
│   ├── Dockerfile.inference          # Inference container definition
│   ├── docker-compose.yml            # Multi-container orchestration
│   └── .dockerignore                 # Build optimization
│
├── 🤖 AUTOMATION
│   ├── Makefile                      # Command automation (20+ commands)
│   ├── download_dataset.sh           # Dataset downloader
│   ├── run_inference.sh              # Inference wrapper
│   └── example_workflow.sh           # Interactive tutorial
│
├── 📚 DOCUMENTATION
│   ├── README.md                     # Main documentation (updated)
│   ├── QUICK_REFERENCE.md            # Quick commands
│   ├── DOCKER_GUIDE.md               # Docker usage guide
│   ├── IMPLEMENTATION_SUMMARY.md     # Implementation details
│   └── DOCKER_SETUP_COMPLETE.md      # Setup confirmation
│
├── 🧠 SOURCE CODE
│   ├── Training.py                   # Training script
│   ├── inference.py                  # Inference script (enhanced)
│   ├── requirements.txt              # Python dependencies
│   ├── mrcnn/                        # Mask R-CNN library
│   └── configs/                      # Configuration files
│
└── 💾 DATA DIRECTORIES
    ├── models/                       # Trained model weights
    ├── logs/                         # Training logs & checkpoints
    ├── test_images/                  # Your test images
    ├── inference_results/            # Output results
    └── Root Images/                  # Training dataset

================================================================================
                           GETTING STARTED
================================================================================

3 SIMPLE STEPS:

  1️⃣  make build-all      # Build Docker images (10-15 min)
  2️⃣  make train          # Train model (3-6 hours with GPU)
  3️⃣  make inference      # Run predictions on your images

OR USE INTERACTIVE GUIDE:

  ./example_workflow.sh   # Step-by-step guided setup

================================================================================
                         DOCUMENTATION LINKS
================================================================================

📖 Full Guide:          README.md
⚡ Quick Start:         QUICK_REFERENCE.md
🐳 Docker Guide:        DOCKER_GUIDE.md
📋 Implementation:      IMPLEMENTATION_SUMMARY.md
✅ Setup Status:        DOCKER_SETUP_COMPLETE.md

================================================================================
                              SUCCESS! 🎉
================================================================================

Your root phenotyping system is now fully dockerized and automated!

All you need to do is:
  1. Build: make build-all
  2. Train: make train
  3. Infer: make inference

Happy phenotyping! 🌱

================================================================================
